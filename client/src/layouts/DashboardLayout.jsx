import { LogOut, Menu } from 'lucide-react'; import { Outlet } from 'react-router-dom'; import { Sidebar } from '../components/layout'; import { Avatar, Button } from '../components/ui'; import { useAuth } from '../hooks/useAuth'; export function DashboardLayout({ role }) { const { user, logout } = useAuth(); return <div className="flex min-h-screen"><Sidebar role={role} /><div className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b border-zinc-800 px-4 sm:px-8"><button className="lg:hidden"><Menu /></button><span className="text-sm capitalize text-slate-400">{role} portal</span><div className="flex items-center gap-3"><Avatar name={user?.name || 'User'} /><Button variant="ghost" size="sm" onClick={logout} aria-label="Sign out"><LogOut size={17} /></Button></div></header><main className="mx-auto max-w-7xl p-4 sm:p-8"><Outlet /></main></div></div>; }
